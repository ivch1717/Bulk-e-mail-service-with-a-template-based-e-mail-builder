import { Routes } from '@angular/router';
import { PreparationPage } from './preparation-page/preparation-page';

export const routes: Routes = [
  { path: 'preparation', component: PreparationPage }
];
