services:
  letterConstructor-service:
    build:
      context: .
      dockerfile: LetterConstructor/ConstructorApi/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
    ports:
        - "7201:8080"


volumes:
  pgdata: